let input;
const numbers = [];
let total = 0;

while (input !== null) {
  input = prompt("number");
  numbers.push(input);
}
for (number of numbers) {
  total += Number(number);
}

console.log(`sum = ${total}`);
